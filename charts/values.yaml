# Default values for karpenter.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: ghcr.io/pfeifferj/karpenter-provider-ibm-cloud/controller
  tag: latest
  pullPolicy: IfNotPresent
  digest: "sha256:2f97b9e74575a45c09e01dbb708bb8cb37b0ba8c4b62f3a7d27a3aec3a506128"
  # Overrides the image tag whose default is the chart appVersion.

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 80

resources: {}
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}

# Pod Disruption Budget configuration
podDisruptionBudget:
  # The name of the PDB. If not set, it will use the release name
  name: ""
  # Maximum number of pods that can be unavailable
  maxUnavailable: 1

# IBM Cloud credentials configuration
credentials:
  # Required: IBM Cloud API Key for general operations
  ibmApiKey: ""
  # Required: IBM Cloud Region
  region: ""
  # Optional: VPC API endpoint URL
  vpcUrl: "https://us-south.iaas.cloud.ibm.com/v1"
  # Optional: VPC authentication type
  vpcAuthType: "iam"
  # Required: VPC API Key for VPC operations
  vpcApiKey: ""
  # Optional: Global Catalog authentication type
  globalCatalogAuthType: "iam"

# Controller configuration
controller:
  metrics:
    port: 8080
  livenessProbe:
    httpGet:
      path: /healthz
      port: 8081
    initialDelaySeconds: 5
    timeoutSeconds: 1
  readinessProbe:
    httpGet:
      path: /readyz
      port: 8081
    initialDelaySeconds: 5
    timeoutSeconds: 1

# Metrics configuration
metrics:
  # Enable ServiceMonitor creation
  serviceMonitor:
    enabled: false
    additionalLabels: {}
    endpointConfig: {}
